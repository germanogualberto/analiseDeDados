---
title: "R Notebook"
output: html_notebook
---


```{r}
require(ggplot2, quietly = TRUE)
require(GGally, quietly = TRUE)
require(reshape2, quietly = TRUE)
require(dplyr, quietly = TRUE)
library(htmlwidgets)
library(dygraphs)
library(tidyverse)
library(plotly)

```

TODO: AUMENTAR A QUANTIDADE DE SERIES MASCULINAS
Algumas séries com personagens principais masculinos
```{r}
masculino=series %>% filter(series_name %in% 
c("Mr Robot", "Breaking Bad","Vikings", "Gotham"))
sexo="M"
sexo=data.frame(sexo)
masculino=merge(masculino,sexo)
```

TODO: AUMENTAR A QUANTIDADE DE SERIES FEMININAS
Agora, algumas séries com personagens principais femininos
```{r}
feminino=series %>% filter(series_name %in% 
c("13 Reasons Why", "Orphan Black", "How to Get Away with Murder"))
sexo="F"
sexo=data.frame(sexo)
feminino=merge(feminino,sexo)
```

```{r}
series_usadas=rbind(masculino,feminino)
```

Mas como média é bastante influenciada por valores extremos, vamos observar um boxplot dos dados.
TODO: ESCONDER USERRATING NO PLOT

```{r}
ggplot(series_usadas, aes(x = sexo, y = UserRating )) + 
  geom_boxplot() +
  geom_jitter(color = "orange", width = .1, height = 0, alpha = .6)

```


```{r}
plot_ly(data = series_usadas, 
        x = ~ sexo, 
        y = ~ UserRating, 
        type="box",
        boxpoints = "all",
        jitter = 0.3,
        pointpos = 0,
        color = ~as.character(series_name), 
        text = ~series_name) %>% 
  add_boxplot(y = ~ UserRating, 
            type = "scatter",
            mode = "lines+markers",
            color = ~as.character(sexo),
            hoverinfo = "text")

```



Curiosamente, ambos possuem metade das observações de r10 concentrados abaixo de 0.4. Nenhuma novidade, já que isso quer dizer apenas que é dificil fazer um episodio nota 10.

Já pelo outro lado, ambos possuem a segunda metade muito dispersa. Aparentemente não há relação significativa entre a proporção de nota 10 e se o personagem é masculino ou feminino para os dados que eu selecionei.
